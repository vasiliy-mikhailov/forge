# Forge docs audit — 2026-05-01j (ADR 0015 dec 6 + P22 + AU-11)

Re-audit driven by: implementation of ADR 0015 dec 6 (per-role
aggregate scores in audit findings) and introduction of predicate
**P22** (audit md carries the aggregate-scores section). Same-day
tenth walk; suffix `j`.

Predicate **introduced** this walk: P22.
Predicates **walked** this walk: P22 (against this audit).

## Findings — verdict FAIL

(none.)

## Findings — verdict WARN

(none.)

### Carry-over

- **F2 (PTS/EB self-referential rows)** — P19 documented-deferral
  carve-out applies. Same as in 2026-05-01b/c/d/e/f/g/h/i.

## Findings — verdict INFO

### F1. ADR 0015 dec 6 closed: aggregate scores per agentic-md unit

Predicate: meta. Paths:
`scripts/test-runners/_score_history.py`,
`scripts/test-runners/aggregate-scores.py`,
`phase-h-architecture-change-management/audit-process.md`,
`tests/phase-b-business-architecture/roles/test-auditor.md`,
`scripts/test-runners/test-auditor-runner.py`.

**Symptom.** `_score_history.py` gained `aggregate_per_runner(path)`
and `aggregate_per_lab(path)` — both exclude nullable-score
cases (binary inspection tests carry no scalar reward) and roll
the rest into `score_norm = score_sum / score_max_sum`, then
apply the ADR 0015 ladder to the aggregate (`PASS ≥ 0.8`,
`italian-strike 0.5..0.8`, `FAIL < 0.5`). The CLI helper
`aggregate-scores.py` prints the 6-row markdown table (or
`--raw` for JSON). P22 added to audit-process.md asserts the
audit md carries the section. AU-11 + the i_au_11 runner
function score 4/4 against this audit.

**Note.** Today's aggregates: Auditor 0.921 PASS; Wiki PM 1.000
PASS; all 4 labs 1.000 PASS. Auditor's 0.921 (vs. 1.000) reflects
the AU-05/06/07 reward C5/C6 placeholders that return 0.5 instead
of a real agent-fix-quality signal — see Open follow-up #3.

### F2. ADR 0015 dec 6 wording — already aligned

Predicate: meta. Path:
`phase-preliminary/adr/0015-verifiable-agent-rewards.md`.

**Symptom.** Decision point 6 already names the reporting
expectation ("a new INFO finding kind: 'role X averaged 0.72 —
italian-strike band — at this commit'"). This audit operationalises
the expectation by adopting the per-unit aggregate table as the
canonical INFO finding format.

**Note.** No wording change to ADR 0015 dec 6 was required;
implementation matches the spec verbatim.

### F3. Audit picker bug fixed (latent since first suffixed audit)

Predicate: meta. Path:
`scripts/test-runners/test-auditor-runner.py` `latest_audit_path()`.

**Symptom.** The glob pattern was
`audit-[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].md` — strict
YYYY-MM-DD without the same-day suffix. Since 2026-05-01b first
introduced suffixes, every audit-side test (AU-01..04b) had been
running against `audit-2026-05-01.md` regardless of which
suffixed audit was actually latest. AU-11 surfaced this on
first run (FAILed because the strict-match audit lacked the
new section).

**Note.** Glob updated to
`audit-[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]*.md`. AU-01
now correctly reports `audit-2026-05-01j.md`. No prior audit
was wrong — they all referenced their own state correctly —
but the test-side mechanical checks were comparing against
stale data. All audit-side cases re-run clean against the
correct latest-audit target after the fix.

### F4. F1+F2 from 2026-04-30 stay closed (12 of 12)

Predicate: meta. (See audit-2026-05-01f, g, h, i.)

**Symptom.** No regression in any closed finding from
2026-04-30 across the 4 follow-up walks.

**Note.** Documented for completeness; no action needed.

### F5. F5 from earlier walks (PTS/EB self-referential rows) carries over

Predicate: P19. (See F2 above is wrong — this is a separate
finding; PTS/EB rows in catalog.md.)

**Symptom.** Same as in 2026-05-01b/c/d/e/f/g/h/i — PTS and EB
catalog rows reference themselves rather than a downstream
realising trajectory.

**Note.** Documented-deferral carve-out applies; no action
needed today.

## Aggregate scores per agentic-md unit

| Unit                            | Cases (scored / total) | Aggregate score   | Band            | PASS / italian-strike / FAIL |
|---------------------------------|------------------------|-------------------|-----------------|------------------------------|
| Auditor                         | 13 / 13              | 35.0 / 38.0 = 0.921 | PASS            | 13 / 0 / 0                      |
| Wiki PM                         | 17 / 17              | 33.0 / 33.0 = 1.000 | PASS            | 17 / 0 / 0                      |
| rl-2048 lab AGENTS.md           |  4 / 4               | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                      |
| wiki-bench lab AGENTS.md        |  4 / 4               | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                      |
| wiki-compiler lab AGENTS.md     |  4 / 4               | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                      |
| wiki-ingest lab AGENTS.md       |  4 / 4               | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                      |

Note: aggregates exclude nullable-score cases (binary inspection
tests). Auditor's 35/38 reflects the AU-05/06/07 reward functions
that score 5/6 each (C5 and C6 — agent-fix-quality components —
are placeholders today; the runner is not wired to a real agent).
AU-11 added 4 more max points (heading + table + ≥6 rows + 6
canonical units) and 4 actual points; that lifts the aggregate
from 0.912 (12-case baseline) to 0.921 (13-case current).

## Summary

| Verdict | Count |
|---------|-------|
| FAIL    | 0     |
| WARN    | 0     |
| INFO    | 5     |

Predicates walked: **P22** (against this audit). All 6 canonical
units accounted for; AU-11 PASS at 4/4.

## Open follow-ups

1. **21 broken links** — pre-existing; out of scope.
2. **Predicate refinements** queued from 2026-05-01: P15
   cross-check, P17 completeness, P19 carve-out.
3. **Auditor C5/C6 placeholders** — the agent-fix-quality
   components in score_p6_decision_case currently return 0.5*
   placeholders because the runner is not wired to a real agent
   that produces a fix. Lifting these to real signals would
   raise Auditor's aggregate from 0.912 → 1.000.
4. **Score-history rotation** — JSONL files grow unbounded
   (one row per case per logged run). At today's logging cadence
   this is years from being a problem; consider a rotation or
   compaction policy if the files cross 10k rows.
