# Forge docs audit — 2026-05-01h (closure pass for the 6 P20 WARNs)

Re-audit driven by: closing the 6 single-hit P20 WARN findings
from [`audit-2026-05-01g.md`](audit-2026-05-01g.md). Same-day
eighth walk; suffix `h`.

Predicate walked: **P20** (full working tree, post-edits + post-
algorithm-refinement).

## Findings — verdict FAIL

(none.)

## Findings — verdict WARN

(none from this walk.)

### Carry-over (not re-walked this audit)

- **F2 (PTS/EB self-referential rows)** — P19 documented-deferral
  carve-out applies. Same as in 2026-05-01b/c/d/e/f/g.

## Findings — verdict INFO

### F1. P20 walk now reports 0 files / 0 hits across the working tree

Predicate: P20. The 6 WARN findings from 2026-05-01g are all
closed. Closures came in two paths:

**Path 1 — algorithm refinement (1 of 6 closed mechanically).**

`scripts/test-runners/test-auditor-runner.py` `p20_findings`
extended: the repeated-title check now also strips occurrences
inside `"..."` (quoted labels / row names) and inside backticks
(code / identifier references), in addition to the existing
markdown-link `[...]` exclusion. Rationale: a quoted reference
to a catalog row label is a *pointer* to that row, not a
restatement of the file's H1. AU-10 still scores 4/4 — the
fixture's repeated-title hit is plain prose, not quoted.

Closed via Path 1:
- `phase-requirements-management/wiki-requirements-collection.md`
  — the hit was on `row "Wiki requirements collection"` (a
  quoted catalog-row label). Now exempt.

**Path 2 — sentence rewrites (5 of 6 closed editorially).**

| File                                                              | Edit                                                                                                                                                          |
|-------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `phase-f-migration-planning/migration-plan.md`                    | H1 renamed `# Migration plan` → `# Implementation and Migration Plan` (matches the TOGAF Phase F deliverable name). Body sentence collapsed accordingly.    |
| `phase-h-architecture-change-management/trajectory-model.md`      | "*The Level 1 / Level 2 trajectory model is …*" → "*The Level 1 / Level 2 mechanism is …*" — the title-restating noun phrase replaced with a synonym.        |
| `phase-preliminary/architecture-method.md`                        | "*This is **forge's chosen architecture method**…*" → "*This is **forge's chosen approach to architecture**…*" — equivalent meaning, no verbatim H1 echo. |
| `phase-preliminary/architecture-repository.md`                    | "*TOGAF's Architecture Repository has six components.*" → "*TOGAF defines six repository components.*" — drops the redundant "Architecture".                |
| `phase-preliminary/architecture-team.md`                          | "*the architecture team is one person*" → "*the team is one person*" — drops the redundant "architecture".                                                  |

**Symptom.** `python3 scripts/test-runners/test-auditor-runner.py
AU-10` still PASS at 4.0/4.0 (algorithm intact). Full P20 walk
across the working tree (minus carve-outs): **0 files with hits**,
0 FAIL, 0 WARN.

### F2. Algorithm refinement covered by AU-10 regression-test

Predicate: meta. Path:
`tests/phase-b-business-architecture/roles/test-auditor.md`
case AU-10.

**Symptom.** The repeated-title-with-quoted-exclusion change
preserves AU-10's 4/4 score because the fixture's repeated-title
hit (`A deliberately bloated fixture` in plain prose) is not
inside quotes, backticks, or markdown links. AU-10 thus catches
algorithm regressions in either direction:
- *false-positive direction*: if a future edit removes the
  quoted/backticked/link-text exclusion, the algorithm would
  start flagging legitimate cross-references; AU-10 would still
  PASS but a P20 walk would generate noise. Caught in the
  *audit*, not in AU-10 alone.
- *false-negative direction*: if a future edit removes the
  filler-phrase / orphan-header / repeated-title detector,
  AU-10 fails immediately on the next runner pass.

The two layers (AU-10 self-test + audit walk) are
complementary; together they bound the algorithm.

### F3. F1+F2 from 2026-04-30 stay closed (12 of 12)

Predicate: meta. Same close-out as in 2026-05-01f/g; no
regression.

### F4. F5 (PTS/EB self-referential rows) carries over

(See F2 above; documented-deferral carve-out applies.)

## Summary

| Verdict | Count |
|---------|-------|
| FAIL    | 0     |
| WARN    | 0     |
| INFO    | 4     |

Predicates walked: **P20** (full working tree, post-edits +
post-algorithm-refinement).

## P20 walk results — full table

(Empty — 0 files with P20 hits.)

## Open follow-ups

1. **21 broken links** — pre-existing in wiki-ingest prompts,
   phase-d/architecture.md, phase-f experiments. Out of scope.
2. **Predicate refinements** queued from 2026-05-01: P15
   cross-check, P17 completeness, P19 carve-out.
3. **Score-history logging** (ADR 0015 dec 5) still
   unimplemented.
4. **P20 algorithm follow-ups (deferred)**: the algorithm
   currently treats any `.md` under the working tree as
   operational unless path-carved-out. Future refinement:
   explicit allow-list of operational md sub-trees so a stray
   top-level note doesn't accidentally enter the walk.
