# Forge docs audit — 2026-05-01e (closure pass for F1 + F2 ADR rewrites)

Re-audit driven by: closing F1 and F2 from
[`audit-2026-04-30.md`](audit-2026-04-30.md) — the two ADRs
that contained forbidden "Superseded by" patterns. Same-day
fifth walk; suffix `e`. Run by the
[Auditor role](../phase-b-business-architecture/roles/auditor.md).

Predicates walked: **P7, P10**. P5 and the carry-overs (F10, F11)
remain WARN; not addressed in this commit.

## Findings — verdict FAIL

(none.)

## Findings — verdict WARN

### F1. F10 (lab AGENTS.md tests) carries over

Predicate: P5. 4 lab AGENTS.md files still lack
`tests/.../<lab>/test-AGENTS.md`. Same as F2 in 2026-05-01d.

### F2. F11 (process-spec test) carries over

Predicate: P5. `phase-requirements-management/wiki-requirements-collection.md`
still lacks a test at the canonical path. Same as F3 in 2026-05-01d.

## Findings — verdict INFO

### F3. F1+F2 from 2026-04-30 closed (Superseded-by patterns eliminated)

Predicate: P7. Paths:
- `phase-c-information-systems-architecture/application-architecture/wiki-ingest/docs/adr/0005-split-transcribe-and-push.md`
  (rewritten to current state).
- `phase-g-implementation-governance/adr/0003-make-per-service-delegation.md`
  (deleted).

**Symptom.** `grep -rn 'Superseded by' --include='*.md' phase-*`
now returns no hits in operational files (only the rule
definitions in architecture-method.md and process.md, plus prior
audit reports that quote the violations).

**Note.** Closures performed via two distinct paths per the
audit-process operating rules:

- **ADR 0005 rewritten** to current state. The core decision
  (two containers, two repos, deploy keys, debounce) is still
  operational; what was stale was the *meta-annotations* —
  the "Path map for current readers" preface, the four "Amended
  by …" status entries, the ~~strikethrough~~ + "Superseded by
  ADR 0006" inline blocks. All replaced with their current-state
  equivalents. The body now reads as if it has always been the
  truth (kurpatov-ingest + lean pusher image are the names today,
  not historical replacements). Length 280 → 245 lines.

- **ADR 0003 deleted.** The ADR itself was marked superseded by
  ADR 0007 in its Status block; the two-level Make layout that
  decision documented is preserved as a pattern but the specific
  SERVICES list (`caddy mlflow rl-2048 kurpatov-wiki`) and
  no-prefix container-name convention are gone. Information now
  lives in `lab-AGENTS-template.md` + root `README.md`; ADR 0007
  is the architecturally-active record. Per architecture-method
  P10, ADR-numbering gaps are INFO (not FAIL); future contributors
  see ADR 0003 in git history.

### F4. ADR-numbering shows gap at 0003 in phase-g

Predicate: P10. Path:
`phase-g-implementation-governance/adr/`.

**Symptom.** Post-deletion the phase-g/adr sequence is:
0007, 0010, 0011, 0012 (with 0003-0006, 0008-0009 either never
existed or live in other phase folders). Current numbering
post-this-commit: 0007, 0010, 0011, 0012.

**Note.** Per P10 ("Gap = INFO; Duplicate = FAIL"), gaps are
acceptable. Forge's ADR numbering is monotonic across the entire
repo, not per-folder. Current forge-level ADRs land in whatever
phase folder they belong to; gaps reflect deletions and moves,
not lost history (git keeps the deletion).

### F5. F7 (PTS/EB self-referential rows) carries over

Predicate: P19. Same as in earlier audits; documented-deferral
carve-out applies.

## Summary

| Verdict | Count |
|---------|-------|
| FAIL    | 0     |
| WARN    | 2     |
| INFO    | 3     |

Predicates walked: P7, P10. (Carry-overs from earlier walks not
re-walked.)

## Findings closed since 2026-04-30

| Closed in | Was finding in 2026-04-30 | Predicate | Action |
|-----------|---------------------------|-----------|--------|
| 2026-05-01d (8 of 12) | F3, F4, F5, F6, F7, F8, F9, F12 | P11 + P6 | mechanical text edits |
| 2026-05-01e (this audit) | F1, F2 | P7 | ADR rewrite + delete |

10 of 12 FAIL findings from 2026-04-30 are now closed. 2 carry
over as WARN (F10 lab tests + F11 process-spec test); both need
non-trivial test-authoring commits.

## Open follow-ups

1. **F1 (this audit) + F2 (this audit)** — F10 + F11 from
   2026-04-30. Author 4 lab `test-AGENTS.md` + 1 process-spec
   test (or amend ADR 0013 to declare role-test coverage
   sufficient).
2. **21 remaining broken links** (was 22; the ADR-0003 deletion
   didn't break any inbound references, and the rewrite of ADR
   0005 introduced no new broken paths). All 21 are in
   pre-existing files unrelated to recent edits — wiki-ingest
   prompts, phase-d/architecture.md, phase-f experiments, etc.
3. **Predicate refinements** queued from 2026-05-01: P15
   cross-check, P17 completeness, P19 carve-out.
4. **Score-history logging** (ADR 0015 dec 5) still
   unimplemented.
