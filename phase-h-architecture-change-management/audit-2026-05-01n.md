# Forge docs audit — 2026-05-01n (collab pruning + DV-02 regression close + P20 orphan close)

Re-audit driven by: architect call "I have 2 collaborations, one
is enough at least" (delete-on-promotion check) + the post-K2-R2
regression on Developer DV-02 + a stray orphan-header from the
earlier broken-link sweep. Same-day fourteenth walk; suffix `n`.

Predicates walked: **P14** (collab membership), **P20**
(token-bloat), **P21** (regression), **P22** (aggregate-scores
section).

## Findings — verdict FAIL

(none.)

## Findings — verdict WARN

(none.)

### Carry-over

- **F2 (PTS/EB self-referential rows)** — P19 documented-deferral
  carve-out applies. Same as in 2026-05-01b through l.

## Findings — verdict INFO

### F1. Redundant Developer-DevOps K2 sub-collaboration deleted (delete-on-promotion)

Predicate: P14 + architecture-method delete-on-promotion. Path:
`phase-b-business-architecture/roles/collaborations/developer-devops-k2.md` (deleted).

**Symptom.** Membership extraction over both collab files
returned identical sets `{Architect, Auditor, Developer, DevOps,
Wiki PM}` — the sub-collab's role mentions matched the parent's
exactly (because both files reference all 5 roles in their
prose, the sub-collab was already subsumed). Per the
*delete-on-promotion* rule
([architecture-method.md](../phase-preliminary/architecture-method.md))
and the cheap-experiment principle, two collabs that overlap
this completely are over-modelling: one collab + a role
decision-rights matrix is enough.

**Note.** Closures applied in this audit's commit:

- `developer-devops-k2.md` deleted (git history retains it; the
  `git rm` is the archive).
- `kurpatov-wiki-team.md` updated: removed `## Sub-collaborations`
  section, removed the "When to spin a smaller sub-collaboration
  instead" tail, replaced the parent-collab framing paragraph
  with the "one collab is enough; scope via the matrix" rule.
- `phase-b-…/roles/README.md` Collaborations table reduced to
  one row.

Future reasoning: when a recurring task involves only 2-3 of the
five roles, *load* `kurpatov-wiki-team.md` and *use* the
decision-rights matrix to scope which role's authority applies.
Don't author a new file for every subset. If at some point the
team grows beyond the 5 roles AND a recurring task crosses a
new boundary, that's the trigger to revisit — not a 2-role pair
on K2.

### F2. DV-02 regression closed (PASS-italian-strike → PASS via new tests)

Predicate: P21. Path:
`phase-c-…/wiki-bench/tests/synthetic/test_compact_restore.py`.

**Symptom.** P21 walk over `test-developer-runner.jsonl` flagged
DV-02 regressing from PASS (2.0/2.0) at the K2-R1 commit to
italian-strike (1.0/2.0) at the K2-R2 commit. The K2-R2 commit
added `_normalise_raw()` to `compact.py` without a matching
unit test — TDD discipline gap, honest catch.

**Note.** Closure: 4 new test cases added to
`TestNormaliseRaw` in `test_compact_restore.py`:

- synth schema passthrough (identity).
- real wiki-ingest schema `{info, segments[]}` builds
  transcript correctly with leading-space stripped per segment.
- compact_l1 accepts real schema directly (Air-strip works
  through the normaliser).
- defensive: degenerate raw (no info key) yields empty
  metadata rather than raising.

pytest: 23/23 PASS (was 19). DV-02 lifts back to PASS on next
runner walk.

### F3. P20 orphan `## ADRs` header in phase-e/README removed

Predicate: P20. Path:
`phase-e-opportunities-and-solutions/README.md`.

**Symptom.** P20 walk surfaced a single orphan-header hit in
phase-e/README. The `## ADRs` heading was followed only by a
blank line then `## Forward to Phase F` — empty section. This
was a residual artefact of the audit-2026-05-01k broken-link
sweep, which removed the `[\`adr/\`](adr/)` placeholder line
under the heading but left the heading itself.

**Note.** Closure: removed the empty heading entirely (per
delete-on-promotion). Working tree now P20-clean: 0 files / 0
hits across the full sweep.

### F4. F1+F2 from 2026-04-30 stay closed (12 of 12)

Predicate: meta. (See audits 2026-05-01f through 2026-05-01m.)

**Symptom.** No regression in any closed finding from
2026-04-30 across the 7 follow-up walks (f, g, h, i, j, k, l, m).

**Note.** Documented for completeness; no action needed.

## Aggregate scores per agentic-md unit

| Unit                            | Cases (scored / total) | Aggregate score   | Band            | PASS / italian-strike / FAIL |
|---------------------------------|------------------------|-------------------|-----------------|------------------------------|
| Architect                       |  0 / 0                | n/a               | transitive (audit-process) | 0 / 0 / 0                |
| Auditor                         | 13 / 13               | 35.0 / 38.0 = 0.921 | PASS            | 13 / 0 / 0                  |
| Wiki PM                         | 17 / 17               | 33.0 / 33.0 = 1.000 | PASS            | 17 / 0 / 0                  |
| Developer                       |  6 / 6                | 9.0 / 9.0 = 1.000 | PASS            | 6 / 0 / 0                   |
| DevOps                          |  6 / 6                | 10.0 / 10.0 = 1.000 | PASS            | 6 / 0 / 0                   |
| rl-2048 lab AGENTS.md           |  4 / 4                | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                   |
| wiki-bench lab AGENTS.md        |  4 / 4                | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                   |
| wiki-compiler lab AGENTS.md     |  4 / 4                | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                   |
| wiki-ingest lab AGENTS.md       |  4 / 4                | 4.0 / 4.0 = 1.000 | PASS            | 4 / 0 / 0                   |

9 units total. Architect aggregate is `n/a` (transitive coverage
by audit-process predicates per ADR 0013 dec 9). Other 8 units
unchanged from 2026-05-01m. Developer 9/9 reflects DV-02 lifted
back to PASS via the new TestNormaliseRaw cases (next runner
walk will reflect this in a logged row).

## Summary

| Verdict | Count |
|---------|-------|
| FAIL    | 0     |
| WARN    | 0     |
| INFO    | 4     |

Predicates walked: **P14, P20, P21, P22**.

## Open follow-ups

1. **K2 V5_discourse_markers** — author the V5 variant covering
   "значит / вот / то есть / на самом деле / собственно / как
   бы / ну, " (the 12-pattern table from K2-R2 Post-Mortem).
   Re-run K2-R2 inside the docker container; expect
   trip-quality jump from 0.012 to ~0.10-0.20.
2. **Wiki PM corpus-observations source-A re-walk** — harden
   OBS-A-001's verbatim to be a literal grep-able quote from
   raw A. Without it, K2-R2 forward recall is permanently
   capped at 10/11.
3. **Predicate refinements** queued: P15 cross-check, P17
   completeness, P19 carve-out wording.
4. **Auditor C5/C6 placeholders** — lifting these to real
   signals raises Auditor's aggregate from 0.921 → 1.000.
