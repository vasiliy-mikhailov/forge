# Forge docs audit — 2026-05-01c (closure pass for F5)

Re-audit driven by: closing the last remaining WARN finding from
[`audit-2026-05-01.md`](audit-2026-05-01.md) — the `### Reward`
section roll-out across 22 test cases per ADR 0015. Same-day
third walk; suffix `c` distinguishes from the morning audit and
the first closure pass (`b`). Run by the
[Auditor role](../phase-b-business-architecture/roles/auditor.md).

Predicates walked: **P14, P15, P16, P17, P18, P19**.

Predicates **P1–P13** carry over from
[`audit-2026-04-30.md`](audit-2026-04-30.md) — none of those
findings have been fixed in this commit.

## Findings — verdict FAIL

(none.)

## Findings — verdict WARN

(none.)

## Findings — verdict INFO

### F1. F5/F1 (Reward-section roll-out) closed

Predicate: P17. Paths:
- `tests/phase-b-business-architecture/roles/test-auditor.md` —
  AU-01..AU-09 all 9 cases now have `### Reward` sections
  (8 newly authored in this commit; AU-05 was the worked
  example landed in `d68cc54`).
- `tests/phase-b-business-architecture/roles/test-wiki-pm.md` —
  WP-01..WP-14 all 14 cases now have `### Reward` sections
  (6 with mechanical reward functions; 8 marked
  `PENDING (no mechanical reward function)` per ADR 0015
  decision point 3 — those 8 are the per-line classification
  cases that need an LLM-as-judge harness).

**Symptom.** P17 walk now reports 23/23 cases with Reward
sections. Was 1/23 in 2026-05-01 morning audit.

**Note.** F5 from 2026-05-01 closed (and the same-named
re-occurrence F1 from 2026-05-01b closed).

### F2. F7 (PTS/EB self-referential rows) carries over

Predicate: P19.

**Symptom.** Same as F7 in 2026-05-01 + F7 in 2026-05-01b.
Documented-deferral carve-out applies; both Goals retain their
deferral-placeholder rows in the catalog.

**Note.** Carry-over INFO. Will need a real trajectory once the
blockers (PTS: user count > 1; EB: first paying user) clear.

### F3. F9 (AU-05 reward unchanged at 5/6) carries over

Predicate: meta.

**Symptom.** Same as F9 in 2026-05-01 + F9 in 2026-05-01b.
The C5/C6 placeholders haven't been wired through a real fix
paragraph yet; AU-05 still scores 5.0/6.0 (PASS, no
Italian-strike). The 8 newly-added AU Reward sections in this
commit do not yet have runner-side scoring (they describe the
reward function in the spec; the runner only computes for
AU-05). Wiring those is the next runner-side commit.

**Note.** Carry-over INFO. Italian-strike state still not
observed in production.

### F4. WP-07..WP-14 are PENDING-no-mechanical-reward (per ADR 0015 dec 3)

Predicate: P17 + meta. Paths: 8 cases in test-wiki-pm.md.

**Symptom.** Per ADR 0015 decision point 3, when a reward
property cannot be computed mechanically, the test case must say
so explicitly with `PENDING (no mechanical reward function)`.
The 8 WP-NN per-line classification cases all do — their Reward
sections list the components an LLM-as-judge harness would score
once it lands, and the runner returns `SKIP` for them.

**Note.** This is consistent with the ADR — not a violation. It
documents that 8 of 14 WP cases are PENDING by design until
the harness exists. P17 should treat PENDING-no-mechanical-
reward as conformant; the predicate's signal already does
(checks for `### Reward` block presence, regardless of whether
it's mechanical or PENDING).

## Summary

| Verdict | Count |
|---------|-------|
| FAIL    | 0     |
| WARN    | 0     |
| INFO    | 4     |

Predicates walked: P14, P15, P16, P17, P18, P19.

Predicates with no findings:
- All 6 motivation-layer predicates (P14–P19) walk clean of FAIL
  or WARN. The 4 INFO findings are all carry-overs or
  documented-design states, not violations.

## Findings closed since 2026-05-01b

| Closed in this audit | Was finding in 2026-05-01b | Predicate |
|----------------------|---------------------------|-----------|
| F1 (this audit, INFO) | F1 (WARN) | P17 |

The last open WARN from the 2026-05-01 morning walk is closed.

## Open follow-ups

1. **Runner-side scoring for the 8 newly-added AU Reward
   sections.** AU-01..AU-04 and AU-06..AU-09 each have a
   declared reward function in the spec; the runner only
   computes for AU-05 today. Wire each runner case to call its
   spec's components and emit a score per the ADR-0015 verdict
   ladder.
2. **Runner-side scoring for the 6 mechanical WP Reward
   sections.** WP-01..WP-06 likewise: the spec is in place but
   the runner returns binary PASS today, not a score.
3. **LLM-as-judge harness** for WP-07..WP-14 (8 PENDING cases)
   — needed to lift those from SKIP to PASS / FAIL / PASS-
   italian-strike.
4. **Predicate refinements** queued from 2026-05-01 still
   unaddressed (P15 cross-check; P17 completeness; P19
   carve-out logic).
5. **P1–P13 carry-overs from 2026-04-30** — 12 FAIL findings
   remain open; not addressed in any audit since.
