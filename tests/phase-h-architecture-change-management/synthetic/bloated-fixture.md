<!-- p20: deliberate-bloat-fixture -->
# A deliberately bloated fixture

This file exists ONLY as the input fixture for AU-10 (token-bloat
detection synth test). Do not link to it from operational md and
do not treat its content as authoritative — it is intentionally
filled with the patterns P20 forbids so the auditor's algorithm
has something to catch.

## Purpose

A deliberately bloated fixture (note: the H1 title is restated
here as plain text, which P20 forbids — an intentional violation
to exercise the "repeated-title" check).

## Background

As mentioned above, the purpose of this file is to be bloated.
Please note that we will state this fact again. As we have seen,
the file is bloated. To recap: this file is bloated.

## Section with no content

## Another empty section

## Yet another empty section

## Conclusion

In conclusion, this fixture is intentionally low-density. It
demonstrates several distinct bloat patterns: filler phrases,
orphan headers stacked back-to-back, and the H1 title repeated
as plain text. Each pattern is exactly what P20 walks the working
tree to detect.
